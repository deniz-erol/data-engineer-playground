version: 2

models:
  - name: int_customer_orders
    description: "Intermediate model that joins customers and orders"
    columns:
      - name: customer_id
        description: "Primary key for customers"
        tests:
          - unique
          - not_null